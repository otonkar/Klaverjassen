from klaverjas.models import Match, Game, GamePlayer, Slag, Troef

# ModelA.objects.create(phone=data['phone'], user_id=1)

# Game.objects.create(matchID = 'Match1', gameStatus_gamestatus = 'niet gestart')

Match.objects.create